from setuptools import setup, find_packages

setup(
    name='naam',
    packages=find_packages(),
)


# setup(
#     name='naa',
#     version='0.1',
#     packages=find_packages(),
#     install_requires=[
#         # list your project's dependencies here
#         # e.g., 'requests>=2.22.0'
#     ],
#     entry_points={
#         'console_scripts': [
#             'my_project=my_module.some_code:main',  # This creates an executable named `my_project` that runs the `main` function in `some_code.py`
#         ],
#     }
# )